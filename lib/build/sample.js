'use strict';

const sample = ({
  instrumentName,
  note,
  absolutePath,
  relativePath,
  extension,
}) => ({
  instrumentName,
  note,
  extension,
  absolutePath,
  relativePath,
});

module.exports = sample;
